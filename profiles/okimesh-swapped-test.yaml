# TEST profile (schema v2) — brokers SWAPPED (slot 0 = mqtt2, slot 1 = mqtt1)
# vs the standard profile, so applying one then the other shows change rows both
# ways. Anonymous TCP, no secrets, no broker enabled. Not for real use.

schema_version: 2
name: OKIMesh brokers swapped (TEST)

mqtt:
  brokers:
    - slot: 0
      url: mqtt://mqtt2.okimesh.org:1883
      port: 1883
      transport: tcp
      auth_type: none
      topic_prefix: meshcore
    - slot: 1
      url: mqtt://mqtt1.okimesh.org:1883
      port: 1883
      transport: tcp
      auth_type: none
      topic_prefix: meshcore
